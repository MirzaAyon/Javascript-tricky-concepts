function work(x, y = 4) {
    return x + y;
}
console.log(work(32);
   //ans : syntax error

   //quiz 10
   Can you access the let variable outside of the block scope ?
    //no

    //    quiz - 11
    JS is a _____ type programming language.
//dynamic
//primitive datatype in JS
//BigInt